#include "uls.h"

void mx_parse(int argc, char **argv) {
    int count = 1;
    t_lit **args = mx_getname(argc, argv, count);
    
    if (args) {
        mx_opendir(&args, ) // нужно доделать опен дир 

        https://github.com/vriabyi/Ucode_clang/blob/master/uls/mx_opendir.c ---> пример опен дира/ 
        теперь нужно будет подтягивать флаги!!!!!
    }
    return;

}
