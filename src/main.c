#include "uls.h"

int main(int argc, char *argv[]) {
    t_head head;

    mx_memset(&head, 0, sizeof(t_head));
        if(argc == 1) {
            // вывести стандартный ls
            //strerror(errno);//
        }
            mx_check_mode(argv, argc);
        //system("leaks uls");
    








    // парсимо флаги
    /* int f1(t_head &head) - зберігає флаги у check_flag */

    // заповнюємо список даними
    /* void f2((argv + те, що повернула функція f1 - кількість флагів), &head)  - */

    // сортуємо
    /* f3(&head) */

    // виводимо
    
}
